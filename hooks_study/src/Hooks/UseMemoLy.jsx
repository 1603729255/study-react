import React from 'react'

export default function UserMemo() {
  return (
    <div>
{/* 
  第二个参数是依赖，如果没有改变就直接拿到缓存的值。
  useMemo 第一个参数是函数，得到一个函数的返回值
  useCallback 直接执行第一个参数的函数
    useMemo(()=>{},[])
    useCallback()
*/}

    </div>
  )
}
